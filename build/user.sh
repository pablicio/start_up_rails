# create a user for log in
sudo useradd assistyouse

# specify a password
sudo passwd assistyouse
# then type your password when prompted

# add the user to sudo group
sudo adduser assistyouse sudo    

